# FortiAIGate 8.0.1 — Helm values override
# Place this file inside gitops/security/fortiaigate/chart/ alongside Chart.yaml.
# ArgoCD will use it as the values override automatically.

# ── Global ────────────────────────────────────────────────────────────────────
global:
  security:
    allowInsecureImages: true   # Required — images come from private registry

  # License mapping: one .lic file per GKE worker node. Updated for cluster
  # recreate 2026-06-19 (system-pool hash 257fde24).
  # NOTE: 3 system nodes exist but only 2 node-locked licenses — the 3rd node
  # (…-gs88) is intentionally unmapped. If a licensed FortiAIGate component
  # schedules there it will report "Invalid"; pin FortiAIGate to the two mapped
  # nodes (nodeSelector/affinity) or add a 3rd license if that happens.
  licenses:
    "gke-aegis-cluster-system-pool-257fde24-36j9": "files/licenses/FAIGCNSD26000104.lic"
    "gke-aegis-cluster-system-pool-257fde24-41g9": "files/licenses/FAIGCNSD26000135.lic"

# ── FortiAIGate image & GPU ───────────────────────────────────────────────────
fortiaigate:
  image:
    repository: us-central1-docker.pkg.dev/gyao-bde-demo/aegis-repo
    tag: V8.0.1-build0031
    pullPolicy: IfNotPresent
    pullSecrets: []

  gpu:
    enabled: false            # Disabled — GPU nodes fully used by KServe models

triton:
  replicas: 1                 # CPU-only — GPU nodes fully used by KServe; slower but functional for demo
  resources:
    requests:
      cpu: 500m
      memory: 4Gi
    limits:
      cpu: "2"
      memory: 8Gi

# ── Storage (API and Core services) ───────────────────────────────────────────
storage:
  size: 8Gi
  storageClass: nfs-rwx
  existingPvName: ""

# ── Ingress ───────────────────────────────────────────────────────────────────
ingress:
  className: nginx
  host: ""
  annotations: {}

# ── TLS ───────────────────────────────────────────────────────────────────────
tls:
  enabled: true
  mountPath: "/etc/tls"
  cert: files/certificate/dflt.crt
  key: files/certificate/dflt.key

# ── PostgreSQL ────────────────────────────────────────────────────────────────
# Fixed passwords prevent secret drift across ArgoCD syncs (demo environment).
postgresql:
  auth:
    password: "FortiAIGateDemo2024!"
    postgresPassword: "FortiAIGateAdmin2024!"
  primary:
    resources:
      requests:
        cpu: 100m
        memory: 1Gi
      limits:
        cpu: "1"
        memory: 4Gi
    persistence:
      size: 8Gi
      storageClass: nfs-rwx

# ── Demo CPU/memory requests ──────────────────────────────────────────────────
# Production defaults are 1000m CPU + 6Gi memory per scanner — too large for
# shared e2-standard-4 nodes. Limits kept at production values to allow bursting.
core:
  resources:
    requests:
      cpu: 100m
      memory: 2Gi
    limits:
      cpu: "1"
      memory: 4Gi

scanners:
  defaultResources:
    requests:
      cpu: 100m
      memory: 1Gi
    limits:
      cpu: "2"
      memory: 2Gi
  sensitive:
    resources:
      requests:
        cpu: 100m
        memory: 2Gi
      limits:
        cpu: "2"
        memory: 10Gi
  anonymize:
    resources:
      requests:
        cpu: 100m
        memory: 2Gi
      limits:
        cpu: "2"
        memory: 40Gi

logd:
  resources:
    requests:
      cpu: 50m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

# ── Redis ─────────────────────────────────────────────────────────────────────
redis:
  master:
    resources:
      requests:
        cpu: 100m
        memory: 512Mi
      limits:
        cpu: "1"
        memory: 2Gi
    persistence:
      size: 4Gi
      storageClass: nfs-rwx
